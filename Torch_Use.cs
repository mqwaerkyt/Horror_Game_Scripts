using UnityEngine;

public class Torch_Use : MonoBehaviour
{
    public bool torchOn;
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        // this script is enabled if torch is in main hand , this script will disable automatically when not in main hand
        // Function of Torch in hand
        print("its enabled");

        // enable task script
        
    }
}
